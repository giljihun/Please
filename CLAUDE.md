# Please! — 프로젝트 규칙

## 프로젝트 개요
팬이 스마트폰만으로 셀럽/선수에게 가상 렌즈 사인 경험을 받을 수 있게 하는 글로벌 iOS 앱.
실제 렌즈 사인 문화를 화면 위에서 재현한다. 상세 설계는 @PLANNING.md 참고.

## 기술 스택
- Swift, iOS (SwiftUI 기본 + 캡처 화면만 UIKit 코어)
- AVFoundation (카메라, 녹화, 합성), Vision (손 추적), SwiftData (라이브러리 메타데이터)

## 아키텍처 원칙
- 앱 쉘/일반 화면: SwiftUI + MVVM
- 사인 세션 화면: UIViewControllerRepresentable로 감싼 UIKit CaptureViewController
  - 이유: AVCaptureVideoPreviewLayer, coalescedTouches/predictedTouches 기반 드로잉 품질
- **녹화와 합성을 분리한다** (2026-08-31 개정, 이슈 #6)
  - 세션 중에는 `AVCaptureMovieFileOutput`으로 **카메라·마이크 원본만** 녹화한다
  - 획은 화면에 그리는 동시에 **캡처 PTS와 함께 타임라인으로 기록**한다
  - 재생·미리보기는 원본 위에 잉크를 **실시간 오버레이**한다 (합성하지 않는다 — 즉시 뜬다)
  - 합성은 **내보낼 때 한 번만** `AVMutableVideoComposition`으로 한다. 결과는 캐시한다
  - 근거: 실시간 합성은 30fps에서 프레임당 33ms 안에 Vision(17ms)·합성·인코딩을 모두 끝내야 한다.
    합성을 미루면 이 예산 제약이 사라지고, 펜 색·굵기 변경과 재렌더·워터마크 분기도 공짜가 된다
- 화면 캡처(ReplayKit 등)는 쓰지 않는다 — UI 요소는 결과물에 포함하지 않는다
  - **유일한 예외: 제스처 펜 본체**(`drawing` 상태) — 사인하는 도구이므로 UI가 아니라 콘텐츠다
    (2026-08-31 확정, 이슈 #13). `hover`·`uncertain`의 링은 진단용이므로 제외한다

## 제품 규칙 (변경 금지)
- 전면 카메라 사용: 화면을 사인하는 사람 쪽으로 향하게 한다
  - **폰은 누가 들어도 된다** (2026-08-31 확정) — 팬이 들 수도, 셀럽이 들 수도, 거치할 수도 있다.
    기본 입력이 공중 제스처라 사인하는 사람 손이 화면에 닿을 필요가 없기 때문
  - 따라서 **화면 터치를 전제한 입력은 설계하지 않는다** — 사인하는 사람이 화면에 닿는다는
    보장이 없다. 터치 모드는 폴백으로만 유지한다
- 사인은 2D 평면에 그려진다 (AR 3D 공간 아님)
- **입력 모드: 제스처 모드(Vision 손 추적)가 기본, 직접 터치는 폴백**
  - 근거: 화면에 닿은 손은 전면 카메라와 같은 평면에 있어 화각 밖 — 사인하는 손이 영상에 담기지 않는다.
    렌즈 사인 문화의 핵심 장면(렌즈 앞에서 움직이는 손)은 공중 제스처로만 재현된다 (2026-08-23 실기기 검증, 이슈 #17)
  - 터치 모드는 조명 불량·Vision 실패 시 폴백으로 유지
  - **자동 전환 로직은 만들지 않는다** (2026-08-26 확정) — Vision 처리 시간을 재서
    터치로 넘기는 식의 판단은 스코프 밖. 폴백은 사용자가 고르는 것이지 앱이 정하는 게 아니다
  - **제스처 생명주기는 Apple 공간 드로잉 방식** (2026-08-26 확정, 이슈 #23) —
    검지 끝은 펜 위치, pinch는 pen-down, release는 즉시 pen-up이다. pinch 판정이 불확실한
    좌표는 바로 잉크로 넣지 않고 짧게 보류한다
  - **단, timeout은 입력을 잠그지 않는다** (2026-08-26 개정, 이슈 #26) —
    당초 "timeout 뒤에는 release 확인 전 재시작하지 않는다"였으나 실기기 계측에서
    보류 초과가 지배적 유실 사유로 나왔다. 엄지는 쥐면 검지 뒤로 숨으므로 판정 불가는
    예외가 아니라 상시 상태이고, 그때마다 입력이 잠겨 "쥐고 있는데 안 그려지는" 현상이 됐다.
    추적이 흔들린 것과 사용자가 손을 뗀 것은 다르다 — 획은 끊되 입력은 계속 받는다.
    release gate는 생명주기 경계(모드 전환, 캔버스 지우기, 타임스탬프 이상)에만 건다
- 영상은 세션 시작~종료 전체 녹화, 저장 시 랜덤 한글 파일명 자동 생성 (예: "귀여운 다람쥐의 사인")
- **녹화에는 소리가 반드시 포함된다** (2026-08-31 확정) — 사인해주는 순간의 목소리와 현장음이
  그 장면의 절반이다. 앱 이름의 유래("please"라고 말한 팬)부터가 소리의 순간이다
  - 마이크 권한이 필요하다 (온보딩 플로우에 포함)
  - `AVCaptureMovieFileOutput`이 비디오·오디오 동기를 처리한다 (립싱크를 직접 관리하지 않는다)
- **세션 플로우** (2026-08-31 확정, 이슈 #9)
  - 대기 → 시작 버튼 → 카운트다운(3·2·1) → **녹화 시작** → "펜을 쥐어보세요" 가이드
    → 그립 인식 → 가이드 해제 → 사인 → 완료 → 감사 화면 → 보관함
  - **버튼은 녹화 시작, 그립은 사인 시작** — 두 신호의 역할을 겹치지 않게 한다
  - **가이드는 세션당 한 번만 뜬다** — 그립이 풀렸다고 다시 나타나면 사인 중에 화면을 가린다
  - 감사 화면("Thank u My SuperStar")은 **앱 안에서만** 보여준다. 영상에 넣지 않는다
- 내보내기 시 앱 워터마크 적용 (BM은 보류, 워터마크 제거가 유료 후보)
- 녹화 시작 전 카운트다운, 세션 종료는 최소한의 "완료" 버튼

## 디자인 원칙
- 목표: **최대한 아이폰스럽고(HIG 준수) Liquid Glass(글래스모피즘)를 반영한 디자인**
- 커스텀 색/모양 대신 시스템 머티리얼 우선: `.glassEffect()`, `.glassProminent`, 시스템 폰트/컬러
- 본격 디자인 작업은 기능 검증(PoC) 완료 후 — 그 전까지 UI는 최소 플레이스홀더 유지

## 카피 원칙 (중요)
- "렌즈에 사인한다"고 절대 표현하지 않는다
- 카메라 앞에서 사인하면 렌즈에 새기는 "듯한" 경험을 준다고 표현한다
- 예: "카메라 앞에서 사인하면 렌즈에 새기는 듯한 경험을 선사합니다"
- ⚠️ "화면에 사인" 표현은 쓰지 않는다 — 기본 입력이 공중 제스처로 바뀌었으므로 사실과 다르다

## 개발자 컨텍스트
- 개발자는 취업 준비 중 — 코드 작성 시 "왜 이 방식인지" 근본 원리를 함께 설명할 것
- 면접에서 설명 가능한 수준의 설계 판단 근거를 코드 주석/커밋 메시지에 남길 것

## ⚠️ Git 규칙 (중요)
- **Claude를 절대 collaborator/co-author로 남기지 않는다**
  - 커밋 메시지에 `Co-Authored-By: Claude ...` 트레일러 금지
  - PR 본문에 `Generated with Claude Code` 등 AI 생성 표기 금지
  - GitHub 레포에 Claude 앱/봇을 collaborator로 초대하지 않는다
  - 모든 커밋/PR은 개발자(giljihun) 단독 저작으로 남긴다

## 이슈 → PR 워크플로우 (Keychy 컨벤션 준수)
- 작업 요청이 오면 **먼저 GitHub 이슈를 생성**하고 시작한다 (이슈 생성은 자동 허용, 푸시/PR 생성/머지는 확인 후)
- **이슈 제목**: `Feature: [스코프] - 설명` — 타입은 Feature/Bugfix/Chore/Style/Refactor (첫 글자 대문자)
- **이슈 본문**: `### 기능 설명` 헤딩 + 간단한 설명 (+ 필요 시 체크리스트)
- **브랜치**: `feature/`, `bugfix/`, `chore/`, `refactor/` 접두사 + 이슈 기반 작업명
- **PR 제목**: `type(scope): 설명` — 소문자 conventional commits (예: `fix(session): 카운트다운 중복 시작 방지`)
- **PR 본문 템플릿**: `## 🎯 PR 내용` / `## 📱 스크린샷 (UI 변경 시)` / `## 🔗 관련 이슈` (`Closes #N`) / `## ✅ 체크리스트` (빌드 성공, 테스트 완료, Self-review 완료)
- **PR 대상**: develop 브랜치
- 머지는 개발자가 직접 한다 (Claude 임의 머지 금지)
- 세션 재시작 시 열린 이슈/PR 조회로 작업 맥락을 복원한다

## 개발자 체크포인트 규칙 (중요)
- **기기 테스트 지점**: 실기기 테스트가 필요한 지점(다음 단계 진행의 전제가 되는 검증)에 도달하면 반드시 개발자에게 알리고, **테스트할 항목을 체크리스트로 정리해 전달**한 뒤 넘어간다
- **코드 점검 지점**: 개발자가 직접 코드를 눈으로 훑고 로직 연결을 이해하고 가는 게 좋은 시점(핵심 구조 확립, 이후 코드의 토대가 되는 부분)이면 먼저 알리고, **읽는 순서와 봐야 할 포인트**를 안내한다

## 코드 리뷰 워크플로우
- **2단 구조**: swift-reviewer(작성 중) → CodeRabbit(PR 단계 백스톱)
  - swift-reviewer는 **어느 정도 진척이 생겼을 때 1~2라운드만** — 매 수정마다 돌리지 않는다
  - 근거: swift-reviewer는 Claude가 프롬프트로 지시하므로 Claude의 맹점을 공유한다.
    실제로 2026-08-23 PR #16에서 swift-reviewer 5라운드가 놓친 실사용 버그 3건을 CodeRabbit이 잡았고,
    swift-reviewer의 잘못된 제안(30fps 설정 순서)도 CodeRabbit이 교정했다
  - 독립적인 두 번째 시선이 핵심이므로, 내부 리뷰를 늘리기보다 PR 리뷰를 신뢰한다
- 리뷰 지적의 위험도는 **기술 위험(크래시/레이스/구조)과 UX 위험(사용자 체감)** 두 축으로 분리해 보고한다
- 리뷰 지적 사항은 심각도 상위부터 수정한다
- CodeRabbit 지적이 로드맵/스코프와 맞지 않으면 사유를 답글로 남기고 skip한다 (묵살 아님, 기록 남기기)
- 리뷰어의 지적에 동의하지 않으면 그 이유를 개발자에게 설명하고 판단을 맡긴다 (묵살 금지)

## 작업 에러 핸들링 프로토콜
- **재시도(Retry)**: 빌드 실패, 테스트 실패는 원인 분석 후 최대 2회 재시도. 같은 접근으로 3번째 실패하면 접근 자체를 바꾼다
- **중단 후 보고(Stop & Report)**: 다음 상황은 임의로 우회하지 말고 즉시 개발자에게 보고한다
  - PLANNING.md의 확정된 설계 결정과 충돌하는 변경이 필요할 때
  - 권한(plist), 프로비저닝, 서명 등 프로젝트 설정 변경이 필요할 때
  - API가 예상과 다르게 동작해 아키텍처 수정이 필요할 때
- **폴백(Fallback)**: 라이브러리/API가 기대대로 안 되면 PLANNING.md의 미결정 사항에 기록된 대안으로 전환하되, 전환 이유를 먼저 설명한다
- **금지**: 테스트를 통과시키기 위한 테스트 삭제/약화, 에러를 숨기는 빈 catch, 실패를 성공으로 보고하는 것
